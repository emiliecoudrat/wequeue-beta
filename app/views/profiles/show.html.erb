
<div class="container text-center margin">
  <h1 class="text-center"><%= @user.name %> </h1>
  <div class="row">
    <div class="col-xs-12">

    <div class="profile">

      <br>
      <p>Prénom : <%= @user.first_name %></p>
      <p>Nom : <%= @user.last_name %></p>
      <p>email : <%= @user.email %></p>

      <%= link_to "Changer mon mot de passe",  edit_pwd_profile_path %>

      <% if @user == current_user %>

      <%= link_to(edit_profile_path, class: "btn btn-default") do %>
        <i class="fa fa-pencil">   Update profile</i>
      <% end %>

    <% end %>

    </div>
    </div>
  </div>
</div>
