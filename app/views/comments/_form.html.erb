<div class="container">
  <div class="row">
    <div class="col-xs-offset-1 col-xs-10 col-sm-offset-3 col-sm-6 col-md-offset-4 col-md-4">
      <%= bootstrap_form_for([@post, @comment] class: 'form') do |f| %>

        <div class="field">
          <%= f.text_area :content %>
        </div>

        <div class="actions">
          <%= f.submit "Save" %>
        </div>

      <% end %>
    </div>
  </div>
</div>



<%= link_to 'Voir toutes les files', lines_path %>